group 'gov.ismonnet.commons'

dependencies {
    compile group: 'io.netty', name: 'netty-all', version: nettyVersion
    compile 'org.slf4j:slf4j-api:1.7.25'

    compile 'javax.inject:javax.inject:1'
}
